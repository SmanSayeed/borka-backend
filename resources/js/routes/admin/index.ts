import businessCategories from './business-categories'
import sizes from './sizes'
const admin = {
    businessCategories: Object.assign(businessCategories, businessCategories),
sizes: Object.assign(sizes, sizes),
}

export default admin